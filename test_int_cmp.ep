external define ep_file_exists with path as Str returning Int

define main returning Int:
    set res to ep_file_exists("/tmp")
    display f"exists result: {res}"
    if res == 1:
        display "Integer comparison worked"
    else:
        display "Integer comparison FAILED"
    return 0
